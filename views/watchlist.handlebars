
    <div class="container">
        <!-- navbar with search -->
        <nav class="navbar navbar-inverse ">
            <div class="container-fluid">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand" href="#">MovieGoers!</a>
                </div>
                <!--        <ul class="nav navbar-nav collapse navbar-collapse">
                <li class="active"><a href="#">Home</a></li>
                <li><a href="#">About</a></li>
                <li><a href="#">Resources</a></li>
            </ul> -->
                <div class="collapse navbar-collapse" id="myNavbar">
                    <ul class="nav navbar-nav navbar-right">
                        <ul class="nav navbar-nav collapse navbar-collapse">
                            <li><a href="/user">Home</a></li>
                            <li><a href="#">About</a></li>
                            <li class="active"><a href="#">Watch List</a></li>
                        </ul>
                        <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown">Welcome, User <b class="caret"></b></a>
                            <ul class="dropdown-menu">
                                <li><a href="#">Profile</a></li>
                                <li role="separator" class="divider"></li>
                                <li><a href="/logout">Sign Out</a></li>
                            </ul>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
        <section id="searchBar">
                       <div class="row">
                            <div class="col col-left-center col-full-xs bottom_divider">
                                <h2>WATCH LIST:<span id="search-term"</span></h2 >
                                <!-- articles will display here -->
                            </div>
                        </div>
          
            <!-- / container-->
        </section>
        <!-- /.section-->
    
        <!-- ============== / divider ================= -->
        
            <div class="row">
                <div class="col col-left-center col-full-xs" id="searchResult">
                    {{!-- {{watchlist}} --}}

                  <ul>
           		     {{#each watchlist}}
                         <form action="/api/watchlist/{{this.id}}?_method=DELETE" method="POST">
                         <input type='hidden' name='movie' value='true'>
                         <button type='submit' class="btn btn-default">Remove!</button>
                                </form>
                    <img src={{poster}} alt="some_text" style="width:150px;height:150px;">
                    <h3>{{title}}</h3>
                    <h5>{{year}}</h5>
           			

                    
           			

                         

                        <div class='eachMovie bottom_divider'>
					 </div>
							
                   {{/each}}

                     </ul>
                    </div> 
                </div>
        {{!-- </section> --}}
       </div>